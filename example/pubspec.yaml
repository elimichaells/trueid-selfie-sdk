name: trueid_sdk_example
description: Example app for the TrueID SDK Flutter plugin.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  trueid_sdk:
    path: ../

flutter:
  uses-material-design: true
